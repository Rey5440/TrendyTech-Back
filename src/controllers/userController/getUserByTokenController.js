const { User } = require("../../db/db");

const getUserByTokenController = async () => {
    
   /*  const existingUser = await User.findOne({ where: { email: auth0UserData.email } })
        
    return user; */

};

module.exports = getUserByTokenController;